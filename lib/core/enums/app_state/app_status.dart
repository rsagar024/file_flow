enum AppStatus { initial, loading, loadMore, success, failure, error }

enum AuthAppStatus {
  initial,
  loading,
  otpSent,
  otpResent,
  authenticated,
  unAuthenticated,
  newUserDetected,
  failure,
  profileUpdating,
  profileUpdateSuccess,
  profileUpdateFailure,
}
